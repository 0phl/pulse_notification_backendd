# Server configuration
PORT=3000

# Firebase configuration - Production Deployment (Render.com)
# Option 1: Paste the entire service account JSON (recommended for production)
# FIREBASE_SERVICE_ACCOUNT_JSON={"type": "service_account", "project_id": "your-project", ...}

# Option 2: File-based approach (development only)
# FIREBASE_SERVICE_ACCOUNT_PATH=./service-account-key.json

# Firebase Database URL (required)
FIREBASE_DATABASE_URL=https://pulse-app-ea5be-default-rtdb.asia-southeast1.firebasedatabase.app

# Optional: Override Firebase Project ID from service account
# FIREBASE_PROJECT_ID=your-project-id
